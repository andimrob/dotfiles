.DS_Store
**/.DS_Store
**/*.bats

# Machine-local secret files, never manage/propagate (auto-sourced by dot_zshrc's zsh/ loader)
.config/zsh/*.local.sh

{{ if ne .chezmoi.os "darwin" }}
.Brewfile
.config/zsh/90-macos.sh
{{ end }}

{{ if not .personal }}
.ssh
{{ end }}
