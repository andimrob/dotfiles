### Installation

If this is a fresh MacOS installation first we need Xcode:

```bash
# If this is a fresh MacOS installation first we need Xcode:
sudo xcode-select --install
chezmoi init
brew bundle
eval $(op signin)
chezmoi apply
```
