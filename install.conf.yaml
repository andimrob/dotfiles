- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.Brewfile:
    ~/.dotfiles: ''
    ~/.editorconfig:
    ~/.gemrc:
    ~/.gitconfig:
    ~/.gitprompt:
    ~/.inputrc:
    ~/.pryrc:
    ~/.psqlrc:
    ~/.railsrc:
    ~/.tmux.conf:
    ~/.vim:
    ~/.vimrc:
    ~/.zlogin:
    ~/.zprofile:
    ~/.zshrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
