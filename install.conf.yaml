- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.aliases:
    ~/.curlrc:
    ~/.dotfiles: ''
    ~/.editorconfig:
    ~/.exports:
    ~/.functions:
    ~/.gemrc:
    ~/.gitconfig:
    ~/.gitignore_global:
    ~/.gitmessage:
    ~/.gitprompt:
    ~/.inputrc:
    ~/.pryrc:
    ~/.psqlrc:
    ~/.railsrc:
    ~/.tmux.conf:
    ~/.vimrc:
    ~/.wgetrc:
    ~/.zlogin:
    ~/.zprofile:
    ~/.zshrc:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
