# Local Aliases Example
# Copy this to ../local/aliases.sh and customize

# Work-specific shortcuts
# alias edit-project="${EDITOR:-vi} ~/src/my-project"
# alias code-project="code ~/src/my-project"

# Database shortcuts
# alias murder-db="psql -c 'select pg_terminate_backend(pg_stat_activity.pid) from pg_stat_activity where pg_stat_activity.datname = '\''my_db_development'\'' and pid <> pg_backend_pid()'"

# Custom bundler shortcuts for specific projects
# alias bxr-project="cd ~/src/project && bundle exec rspec"

# Project-specific navigation
# alias retail='cd ~/src/retail'
# alias main='cd ~/src/main-project'

# Custom editor shortcuts
# alias dotfiles="${EDITOR:-vi} ~/src/dotfiles"
# alias notes="${EDITOR:-vi} ~/Documents/notes"
